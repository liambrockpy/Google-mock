const results = require('../../data')

const findAllByQuery = async (query) => new Promise((res, rej) => {
    res()
})
const findRandomByQuery = async (query) => new Promise((res, rej) => {
    res()
})

module.exports = {
    findAllByQuery,
    findRandomByQuery
}
